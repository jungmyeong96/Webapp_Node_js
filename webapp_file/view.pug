doctype html
html
	head
		meta(charset="utf-8")
	body
		h1 
			a(herf='/topic') Server side JavaScript
		ul
			each name in topics
				li
					a(href='/topic/'+name)= name
		artcle
			h2= title
			= description
		div
			a(href='/topic/new') new
